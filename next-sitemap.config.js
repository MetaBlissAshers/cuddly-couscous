/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://freehqpornxxx.com/',
  generateRobotsTxt: true,
  // Add other options as needed, e.g., exclude: ['/exclude-me'],
}; 